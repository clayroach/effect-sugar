{
  "name": "effect-sugar-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Effect-TS syntactic sugar with for-comprehension style gen blocks",
  "type": "module",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "test:watch": "turbo run test:watch",
    "test:integration": "vitest run --config vitest.integration.config.ts",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint",
    "clean": "turbo run clean && rm -rf node_modules target",
    "preprocess": "node scripts/preprocess.js examples",
    "format": "node scripts/format-gen.js examples",
    "changeset": "changeset",
    "version": "changeset version",
    "publish": "turbo run build && changeset publish",
    "publish:dryrun": "turbo run build && pnpm publish:dryrun:transform && pnpm publish:dryrun:vite",
    "publish:dryrun:transform": "pnpm --filter effect-sugar-transform publish --dry-run --no-git-checks",
    "publish:dryrun:vite": "pnpm --filter effect-sugar-vite publish --dry-run --no-git-checks",
    "publish:dev": "pnpm publish:dev:transform && pnpm publish:dev:vite",
    "publish:dev:transform": "pnpm --filter effect-sugar-transform publish:dev",
    "publish:dev:vite": "pnpm --filter effect-sugar-vite publish:dev",
    "package:vscode": "pnpm --filter effect-sugar run package"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.0",
    "effect": "^3.0.0",
    "turbo": "^2.0.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  },
  "license": "MIT"
}
